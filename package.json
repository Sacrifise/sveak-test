{
  "name": "sveak-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pug src/index.pug -o . -P && vite --host",
    "build": "pug src/index.pug -o . -P && sass src/scss:dist/css --no-source-map && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "pug": "^3.0.3",
    "sass": "^1.97.2"
  }
}
